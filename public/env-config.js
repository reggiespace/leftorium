// Dev/build-time placeholder. In the Dokploy image this file is regenerated
// at container start (see docker/docker-entrypoint.sh) from real env vars,
// so VITE_STRAPI_URL etc. can change without rebuilding the image.
window.__ENV__ = window.__ENV__ || {};
