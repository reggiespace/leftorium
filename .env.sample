VITE_STRAPI_URL=http://localhost:1337
# VITE_STRAPI_TOKEN=your-token-here # Optional if using Public public permissions

# Optional: the landing-page hero photo. Not tied to any Strapi content
# type — just an absolute URL, e.g. from the Garage S3 bucket:
# https://storage.reggiespace.ca/health/leftorium/hero.jpg
# Leave unset to keep showing the labelled placeholder.
# VITE_HERO_IMAGE_URL=
